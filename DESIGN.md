# 形外 Index：随机发现入口与内容完善设计

## 目标

将现有目录工具与已经验证的液态玻璃随机发现体验整合成一个完整静态网站。访客首先通过极简互动随机遇见一种美学，再进入详情或完整目录。目录提供双语标题和有依据的趋势排序；详情页逐步补齐中文正文与可靠来源的图片。

网站继续使用纯静态文件，可直接部署到 GitHub Pages，不引入自建服务器、账号系统或数据库。

## 页面结构

### 随机发现页

- 根路径 `index.html` 作为默认入口。
- 沿用方案一的液态玻璃游移窗口，只保留正式体验需要的内容：品牌、目录入口、随机结果、查看详情和重新随机。
- 移除 `STUDY`、方案切换器和其他实验标识。
- 随机池直接读取完整目录数据，并优先选择拥有本地封面的词条。
- 初始状态不直接显示词条名称；触发揭示后显示英文标题、中文标题，以及查看详情和重新随机两个图标。
- 兼容 `prefers-reduced-motion`，不支持 WebGL 时回退到 Canvas 版本。

### 目录页

- 现有目录迁移到 `catalog.html`。
- 侧栏增加“随机发现”入口，品牌标记也返回随机发现页。
- 删除筛选面板顶部重复分割线。
- 保留类型、年代、气质、首字母、搜索、收藏和加载更多。
- 排序增加“趋势”，默认仍使用 A–Z，避免把估算数据伪装成真实浏览量。

### 详情页

- Header 品牌标记返回随机发现页。
- “返回目录”继续返回 `catalog.html`。
- 标题区显示英文标准名和中文显示名。
- Gallery、收藏、查看原站与来源许可结构保持不变。

## 双语名称

目录数据增加 `nameZh` 和 `translationStatus`：

- `established`：已有稳定中文名称，如“包豪斯”“极简主义”。
- `translated`：根据词义整理出的中文显示名。
- `untranslated`：没有可靠中文译名时保留英文，不制造误导性译名。

第一批覆盖首页热门、现有深度词条和常见 `-core`、年代、地域、艺术运动词汇。卡片使用英文主标题、中文副标题；当中文名与英文相同或尚未确认时不重复显示。

## 趋势排序

Fandom 的精确 Top Pages 和 Top Search Terms 只在管理员 Analytics 中提供，Aesthetics Wiki 的公开 API 不支持 `mostviewed` 或 `pageviews`。站内不能把估算值标成“真实点击量”。

目录增加 `trendScore` 和 `trendReason`，第一版按以下公开信号生成：

1. Aesthetics Wiki 首页公开的 “Popular Aesthetics” 顺序，权重最高。
2. 原站站内反向链接数量，作为长期可见度代理。
3. 本站当前数据完整度，只用于同分排序，避免趋势靠前的页面展示空内容。
4. 同分时按英文名称排序，保证结果稳定。

排序名称显示为“趋势”，界面不显示虚构的浏览数字。另保留“最近浏览”，使用浏览器 `localStorage`，只代表当前用户。

## 正文导入

当前 1,129 个目录项中，6 个拥有深度内容；其余页面只有自动结构概览。新的导入流程按原站章节解析：

- 简介与定义
- 历史和来源
- 视觉特征、色彩与材质
- 时尚、音乐、媒体或生活方式
- 相关美学
- 来源版本、编辑历史和许可

英文原文存入结构化 JSON，中文正文单独存放，避免覆盖来源文本。页面显示中文，来源区保留原条目、修订版本和 CC BY-SA 信息。抓取失败时保留现有概览，并将 `contentStatus` 标为 `summary`，不声称完整。

内容回填分批执行：先处理公开热门与目录首屏，再处理只有一张图或没有正文的词条，最后按字母批量回填其余页面。脚本应可重复运行，并跳过已经完成且修订号没有变化的条目。

## 图片补全

优先从对应 Aesthetics Wiki 条目的 Gallery 与文件页取得图片；不足时只补充拥有明确开放许可的 Wikimedia Commons 图片。普通商业图片搜索结果只用于研究，不直接发布。

每个词条目标为 6–12 张代表图：

- 最大边压缩到 1,200 px，输出 WebP。
- 使用文件哈希和感知哈希去重。
- 过滤站点 UI、头像、图标、低分辨率图片和无关文件。
- 每张图在数据中保存来源 URL、作者、许可和原始说明。
- 页面继续在来源区统一展示许可说明，不恢复逐图悬浮按钮。

为控制 GitHub Pages 体积，单次批处理设置体积预算；超过预算的原站图片保留远程 URL 和占位回退，不无限扩大仓库。

## 数据和文件

- `index.html`：正式随机发现入口。
- `catalog.html`：目录工具。
- `style.html`：详情页。
- `discovery.js`、`discovery.css`、`lens-gl.js`：正式入口交互。
- `catalog.json/js`：增加中文名、趋势和内容状态。
- `content.json/js`：保存结构化正文、中文正文和 Gallery 元数据。
- `work/`：存放可重复执行的抓取、翻译准备、趋势计算和图片处理脚本。

## 验证

- 打开根路径即可进入随机发现页，揭示、换一换和详情跳转都可用。
- 随机发现页、目录页和详情页之间可以双向返回。
- 目录顶部没有重复分割线。
- 趋势排序首先出现原站公开热门词条，且不显示虚构浏览数字。
- 双语标题不会溢出卡片，移动端保持两列或单列可读。
- 完整内容页只显示中文正文；摘要页明确反映内容状态。
- Gallery 图片可打开、关闭和懒加载，失败图片有回退。
- WebGL 不可用和减少动态效果模式下仍可完成核心路径。

## 实施顺序

1. 整合随机发现入口并重写站内链接。
2. 修复目录分割线，加入双语字段和趋势排序。
3. 建立可重复运行的全文与 Gallery 导入脚本。
4. 先补齐公开热门与首屏词条，再按内容缺口逐批回填。
5. 做桌面端、移动端、无 WebGL 和资源失败回归。
